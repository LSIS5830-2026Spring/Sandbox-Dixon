tryon Palace
Old Salem
Fort Macon
Bentonville Battlefield
House 
        name: Tryon Palace
      slug: tryon-palace
      location: "New Bern, Craven County, NC"
      coordinates: { lat: 35.105, lon: -77.045 }
      county: "Craven"
      state: "NC"
      periods: ["Colonial","18th Century"]
      type: "Palace/Statehouse"
      status: "NRHP-listed"
      built_year: "1767-1780"
      opened_to_public: true
      description: "Royal governor’s residence and state capitol building in colonial America."
      resources:
        "https://www.tryonpalace.org"
      image_url: "https://example.org/images/tryonpalace.jpg"
      tags: ["Colonial","Government","Architecture"]
   
If you prefer Markdown collections, you can create _sites/tryon-palace.md with YAML front matter:
    ---
  layout: site
  title: "Tryon Palace"
  slug: "tryon-palace"
  location: "New Bern, Craven {% for s in site.
            {% for r in s.resources %}
              {{ r | split: '/' | last }}{% unless forloop.last %}, {% endunless %}
            {% endfor %}

          {% endif %}

        {% endfor %}

     

    {{ page.title }}

      {{ page.location }}

      {{ page.description }}
      {% if page.image_url %}

      {% endif %}
      {% if page.resources %}
      Resources:
        {% for r in page.resources %}
          {{ r }}{% unless forloop.last %}, {% endunless %}
        {% endfor %}

      {% endif %}

    

assets/css/style.css
    :root {
    --bg: #f7f7f7;
    --card: #fff;
    --text: #222;
    --accent: #1e4d9b;
  }
  body { margin: 0; font-family: Arial, sans-serif; background: var(--bg); color: var(--text); }
  header { background: var(--accent); color: #fff; padding: 1rem; text-align: center; }
  main { padding: 1rem; max-width: 1100px; margin: 0 auto; }
  .grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(280px, 1fr)); gap: 1rem; }
  .card { background: var(--card); border: 1px solid #ddd; border-radius: 8px; padding: 1rem; }
  .card img { width: 100%; height: 170px; object-fit: cover; border-radius: 6px; }
  .meta { color: #555; font-size: 0.9em; }
  a { color: var(--accent); text-decoration: none; }
  a:hover { text-decoration: underline; }
 

Run locally:
  bundle exec jekyll serve
Access:
  http://localhost:4000/
If you added a collection, ensure _config.yml includes it:
    collections:
    sites:
      output: true
 

6) Quick validation tips

Use HTTPS URLs in resources.
Keep coordinates as numbers (lat/lon).
Use slugify for clean URLs (Liquid: {{ s.slug | default: s.
